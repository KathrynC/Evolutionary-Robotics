# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# OS junk
.DS_Store

# generated robot files (regenerated by generate.py)
body.urdf
world.sdf

# sensor data
data/
*.npy

# debug
crash.log
gui_*.py
*.log

# media (root-level; tracked inside artifacts/)
/*.png
/*.mp4
/*.csv
motor_*.txt
motor_vectors.png

# local patch helpers
patch_simulate_constants*.py

# brain.nndf backups (created/restored by scripts)
brain.nndf.backup
brain.nndf.sr_backup

# local backups/scratch
*.bak
*.bak_*
*.broken_*
*_GOOD_*

# ── Artifacts directory ─────────────────────────────────────────
# Track research results (JSON, MD, TXT, PNG) but ignore bulk telemetry
# and old run directories that are too large for git.

# Ignore bulk telemetry directories (314MB+)
artifacts/telemetry/
artifacts/telemetry_*/

# Ignore old run/trace directories
artifacts/traces/
artifacts/traces_old/
artifacts/rules/
artifacts/plots/
artifacts/atlas/
artifacts/jsonl/

# Ignore per-step JSONL files (large)
*.jsonl

# Track everything else in artifacts/ (JSON results, MD notes, PNG figures, TXT leaderboards, TSV data)

# ── Videos (too large for git, 9+ GB) ─────────────────────────
videos/

# ── Third-party PDFs in artifacts ─────────────────────────────
artifacts/Fact Check and Argument Analysis of Paper Draft - DeepSeek.pdf

# ── Claude Code local config ──────────────────────────────────
.claude/

# ── Local scratch notes ───────────────────────────────────────
_notes/
